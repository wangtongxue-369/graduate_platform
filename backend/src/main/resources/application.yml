spring:
  application:
    name: graduate-platform-backend
  config:
    import: optional:file:./config/application-local.yml,optional:file:./backend/config/application-local.yml
  datasource:
    url: jdbc:mysql://118.89.81.131:3306/graduate_platform?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&characterEncoding=utf-8
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: rjxt
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    open-in-view: false
  mail:
    host: smtp.qq.com
    port: 587
    username: 1345169715@qq.com
    password: kyhwvasgnqligjdi
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  servlet:
    multipart:
      max-file-size: 30MB
      max-request-size: 120MB

server:
  port: 8080

app:
  jwt:
    secret: graduate-platform-jwt-secret-key-must-be-at-least-256-bits-long-for-hs256
    expiration-ms: 86400000
  upload:
    mock-interview-dir: uploads/mock-interviews
    mock-interview-max-bytes: 31457280
    mock-interview-allowed-extensions: .pdf,.doc,.docx,.xls,.xlsx,.ppt,.pptx,.txt,.md,.png,.jpg,.jpeg,.gif,.mp3,.wav,.m4a,.mp4,.zip,.rar

tencent:
  cos:
    secret-id: ${COS_SECRET_ID:}
    secret-key: ${COS_SECRET_KEY:}
    bucket: rjxt-1331431081
    region: ap-shanghai
    base-url: https://rjxt-1331431081.cos.ap-shanghai.myqcloud.com

logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%15.15t] %logger{36} - %msg%n%wEx"
  level:
    root: INFO
    com.graduateplatform: INFO
    org.springframework.web: WARN
    org.springframework.security: WARN
    org.hibernate.SQL: WARN
    org.hibernate.orm.jdbc.bind: WARN
