# 就业模块审查发现

## 已有功能

- 后端就业模块包含招聘会、岗位、在线简历、订阅偏好、投递记录、站内就业通知。
- 前端就业页包含就业面板、招聘会与偏好、在线简历、规则推荐和投递跟踪。
- 管理端可维护招聘会和岗位，并手动触发匹配站内通知。
- README 明确就业 v1 不包含 Word/PDF 导出、外部招聘平台同步、邮件/短信/微信推送、AI 推荐。

## 初步问题

- 已补：管理端暴露 `active` 状态编辑能力，可通过启用/停用控制用户端展示。
- 已补：推荐岗位、岗位详情与投递跟踪形成前端闭环，可从岗位卡片/详情页带入 `jobPostingId`、企业和岗位名称。
- 已补：后台就业管理页面中文化，并增加操作反馈。
- 已补：招聘会和岗位详情页，便于答辩展示完整信息层级。
- 已缓解：偏好中的薪资、企业类型没有参与当前通知匹配和推荐打分，前端和 README 已补充当前匹配范围说明。
- 仍需关注：岗位删除仍是硬删除，当前前端增加确认并建议停用；如后续要求更强数据稳定性，可再改为后端软删除策略。
- 验证发现：前端 `npm run build` 通过；后端 `EmploymentModuleIntegrationTest` 当前受题库 `DataInitializer` 和 H2 缺少 `questions` 表影响，测试上下文无法启动。

## 待澄清

- 审查标准已确认：按课程/毕业设计验收优先，重点看功能完整度、演示效果、文档说明和答辩亮点。
- README 中的 Employment v1 non-goals 继续作为审查边界；Word/PDF 简历导出、外部招聘平台同步、邮件/短信/微信推送、AI 推荐不列为当前必补缺陷。
- 审查输出粒度已确认：需要给出后续实现计划，包含问题清单、整改建议、涉及文件和验证方式。
- 实现计划优先级已确认：演示闭环优先，先补一键投递、详情页、后台中文化、状态反馈等答辩展示链路。

---

# 就业模块中文化发现（2026-05-27）

## 已确认事实

- `DataInitializer.initEmploymentData()` 内置就业示例使用英文标题、企业、城市、行业、岗位类型、学历要求和描述，是招聘会与线上申请链接页面展示英文的主要来源。
- `EmploymentService.resolveNotificationSource()` 生成站内提醒时使用英文标题和内容，会在岗位推荐页“站内提醒”区域展示。
- `EmploymentService.recommendationMap()` 返回英文匹配理由，会在岗位推荐卡片展示。
- 就业前端页面主要直接展示后端 `description`、`targetRoles`、`roleType`、`matchReasons`、`notification.content`，前端兜底文案本身已为中文。

## 约束与边界

- 本次不改变 `description`、`applyUrl`、`roleType` 等接口字段名。
- 本次不新增数据库字段、表、索引或迁移框架。
- 本次不引入运行时翻译或新依赖。
- 本次不自动修改已经存在于本地数据库中的旧英文记录。

## 验证发现

- 后端编译通过，说明本次 Java 修改语法与类型检查通过。
- 就业集成测试当前受 H2 对考公实体字段 `year` 的 SQL 兼容问题阻塞，未能验证到就业测试用例执行阶段。
- 前端构建通过；前端 lint 存在既有问题，本次未修改前端代码，未扩大该问题范围。
- 旧英文种子名仅保留在 `DataInitializer` 的存在性判断中，用于避免已有旧种子数据时自动迁移或重复插入，不作为新展示文案写入。

---

# 就业模块旧英文数据库数据替换发现（2026-05-27）

## 根因

- 前端就业页面展示的是后端接口返回的数据库字段，例如岗位 `description` 和招聘会 `description`。
- 旧数据库中已经存在英文种子记录时，仅修改 Java 代码中的新种子文案不会自动改写已有记录。
- 用户本轮明确要求替换数据库中的英文数据，因此可以从原先“只改代码、不迁移旧数据”的边界调整为“仅更新就业旧种子数据”。

## 修改边界

- 只处理精确匹配的旧就业种子记录：`Internet Campus Career Fair / Future Tech`、`Smart Manufacturing Fair / Harbor Equipment Group`、`Java Backend Engineer / Future Tech`、`Product Operations Trainee / Harbor Equipment Group`。
- 不处理管理员或用户后续手工新增的其它英文记录，避免误改真实业务数据。
- 不删除重复记录，不改数据库表结构和接口字段。

## 验证发现

- `mvn -q -DskipTests compile` 通过。
- `mvn -q -Dtest=EmploymentModuleIntegrationTest test` 通过。
- `mvn test -q` 通过。
- `git diff --check` 通过；命令输出包含 Git 对部分文件 CRLF/LF 换行转换的提示，不是空白错误。

---

# 招聘会过期过滤、分页与重复校验发现（2026-05-27）

## 问题事实

- `/job/fairs` 页面原先直接渲染 `/api/job/fairs` 返回的全部招聘会，缺少分页和数量控制。
- 用户端列表只按 `active=true` 查询，不区分已结束、网申已截止和仍可申请的招聘会。
- 当前本地数据中存在重复招聘会记录，主要来自历史种子数据多次按动态时间插入。
- 后台创建招聘会前未使用已有的重复判断能力，后续仍可能继续写入相同标题、企业和开始时间的重复记录。

## 实现约束

- 保持旧接口兼容：未传 `page/size` 的 `/api/job/fairs` 仍返回数组，避免影响已有调用方。
- 新分页响应仅在传入 `page` 或 `size` 时启用，返回 `items/page/size/totalPages/totalItems/includeExpired`。
- 默认分页查询只展示仍可申请的招聘会；选择包含过期记录时，返回 `expired` 和 `statusLabel` 供前端标记。
- 本次不清理已有重复数据，不新增数据库字段，不引入迁移工具。

## 验证发现

- 后端就业集成测试新增覆盖分页过滤、过期状态和重复创建拒绝，并已通过。
- 前端构建通过；`npm run lint` 仍受既有 lint 问题阻塞，本次修改文件未出现在 lint 错误列表。

## 二次修正发现

- 仅在后台创建时做重复校验不能解决已有历史重复数据；用户端列表需要在展示前按业务键去重。
- 原先将“过期”主要理解为网申截止，会导致宣讲已结束但网申未截止的招聘会仍显示在默认列表中；这与用户对招聘会现实状态的理解不一致。
- 当前采用两套状态：`statusLabel` 表示招聘会时间状态，`applyStatusLabel` 表示线上申请状态；`expired` 仅表示招聘会已结束，`applicationClosed` 表示网申已截止。
- 默认用户端列表只展示未结束的招聘会；如果用户选择显示全部，则已结束招聘会仍可展示并标记。
