# 任务计划：就业模块审查

## 阶段 1：预检与事实收集

- 状态：已完成
- 操作：定位就业模块后端、前端、API 封装、README 说明和后端集成测试。
- 关键决策：当前使用 `$deep-interview`，先澄清审查目标，不直接改代码。
- 风险：如果审查标准不同，优先级会明显变化，例如上线质量、课程验收、答辩展示的侧重点不同。

## 阶段 2：用户目标澄清

- 状态：已完成
- 已确认：审查标准按课程/毕业设计验收优先。
- 已确认：README 中 Employment v1 non-goals 继续作为审查边界。
- 已确认：审查报告需要包含后续实现计划。
- 已确认：实现计划以演示闭环优先。

## 阶段 3：形成审查结论

- 状态：已完成
- 预期输出：按优先级列出需要完善或修改的就业模块功能，包含证据文件、原因、影响和建议验证方式。
- 输出：已生成 deep-interview 规格和访谈摘要，并在最终回复中给出审查报告。

## 阶段 4：按建议顺序实现

- 状态：已完成
- 已完成：用户端详情页与一键投递闭环。
- 已完成：管理端中文化、启停状态、删除确认和操作反馈。
- 已完成：匹配范围说明和 README 演示流程。
- 验证结果：`npm run build` 已通过；`mvn -Dtest=EmploymentModuleIntegrationTest test` 未通过，失败原因是 Spring 测试上下文启动时 `DataInitializer` 初始化题库数据，H2 中缺少 `questions` 表，属于题库测试环境/初始化问题，非本次就业前端闭环改动直接引入。
- 后续建议：如需要让后端就业集成测试可单独通过，应先处理测试环境下 `DataInitializer` 与题库表结构初始化顺序问题。

---

# 任务计划：就业模块招聘会与岗位描述中文化

## 阶段 1：需求澄清与事实收集

- 状态：已完成
- 使用 skill：`$deep-interview`，原因是用户明确要求 deep-interview，且需要确认“岗位描述中文化”的范围边界。
- 已确认范围：就业模块展示面全中文化，覆盖内置招聘会/岗位示例数据、站内提醒和匹配理由等固定展示文案。
- 已确认非目标：不改接口字段、不重做页面样式、不做运行时翻译。
- 已确认数据边界：不迁移旧数据库数据；只修改代码内置示例与固定文案。
- 代码事实：英文来源主要在 `DataInitializer.initEmploymentData()`、`EmploymentService` 通知/推荐理由和就业测试数据。

## 阶段 2：最小实现

- 状态：进行中
- 计划修改：
  - 将就业初始化示例招聘会和岗位展示值改为中文。
  - 将就业服务层固定英文错误、通知和匹配理由改为中文。
  - 同步调整就业集成测试中的示例业务数据，保持匹配规则测试语义一致。
- 风险：本次不迁移已初始化数据库旧数据，因此已有本地旧英文记录需要通过管理员编辑或重置测试库才能完全消除展示英文。

## 阶段 3：验证

- 状态：待执行
- 计划验证：
  - 后端聚焦测试：`mvn -Dtest=EmploymentModuleIntegrationTest test`。
  - 前端构建：`npm run build`。
  - 文案检索：确认原英文展示文案仅保留在兼容旧种子数据的存在性判断中。

## 阶段 3：验证结果

- 状态：已完成
- `mvn -DskipTests compile`：通过，后端代码编译成功。
- `mvn -Dtest=EmploymentModuleIntegrationTest test`：未通过，失败点为测试上下文建表时 H2 不接受考公表 `civil_service_posts.year` 字段名；该问题早于本次就业中文化，测试未进入就业业务断言。
- `npm run build`：通过，存在 Vite chunk 体积提示。
- `npm run lint`：未通过，失败来自既有前端 lint 问题，非本次后端中文化变更引入。

---

# 任务计划：替换就业模块旧英文种子数据

## 阶段 1：确认数据来源

- 状态：已完成
- 发现：就业页面直接展示数据库字段 `description`，旧英文岗位来自历史启动时写入的 `career_fairs` 和 `job_postings` 种子数据。
- 决策：用户已明确要求替换数据库英文数据，因此本轮允许对旧种子数据做受限更新。

## 阶段 2：最小实现

- 状态：已完成
- 修改方式：在 `DataInitializer.initEmploymentData()` 启动初始化前，按旧英文标题和公司名称精确匹配历史种子记录，并替换为中文标题、公司、城市、行业、岗位类型、描述等展示字段。
- 范围控制：不删除记录、不修改用户自建非种子记录、不新增字段、不改接口合同。

## 阶段 3：验证

- 状态：已完成
- 已执行：后端编译、就业模块集成测试、后端全量测试和 `git diff --check`。

---

# 任务计划：招聘会过期过滤、分页与重复校验

## 阶段 1：现状核对

- 状态：已完成
- 操作：复核 `/job/fairs` 页面、`/api/job/fairs` 接口、招聘会实体、repository、service、前端页面和既有分页组件。
- 关键发现：用户端招聘会列表一次性展示全部启用记录，包含已截止/已结束记录；数据库中存在同标题、同企业、同地点、同申请链接的重复种子数据。
- 决策：不破坏旧接口兼容性；未传 `page/size` 时保留数组返回，页面显式传分页参数使用新分页响应。

## 阶段 2：最小实现

- 状态：已完成
- 后端计划：新增招聘会分页查询；默认过滤不可申请的过期记录；响应中补充 `expired` 与 `statusLabel`；后台创建/更新招聘会时按标题、企业、开始时间做重复校验。
- 前端计划：`/job/fairs` 页面使用分页接口；新增“过期记录”筛选；卡片展示状态标签；过期记录不展示可点击申请按钮。
- 风险：本次只防止后续后台重复创建，不清理已有重复历史数据；如需清理现有重复记录，需要另做数据处理。

## 阶段 3：验证

- 状态：已完成
- 后端验证：`mvn -Dtest=EmploymentModuleIntegrationTest test` 通过，覆盖分页过滤、过期状态和重复创建拒绝。
- 前端验证：`npm run build` 通过，仅有 Vite chunk 体积提示。
- Lint 验证：`npm run lint` 未通过，失败来自既有未使用变量、Hook 依赖和详情页 `set-state-in-effect` 规则；本次修改的 `CareerFairPage.jsx` 未出现在错误列表。
- 静态检查：`git diff --check` 通过，仅提示部分文件后续可能发生 CRLF/LF 换行转换。

## 阶段 4：重复展示与状态口径修正

- 状态：已完成
- 修正原因：用户反馈页面仍存在重复招聘会，且已过期、进行中等状态与现实时间不一致。
- 后端修正：用户端分页列表改为先查询匹配招聘会，再按标题、企业、地点、申请链接进行业务去重，保留当前最应展示的一条，最后再分页。
- 状态口径：招聘会状态按 `startTime/endTime` 计算为未开始、进行中、已结束；网申状态按 `applyDeadline` 独立计算为可网申或网申已截止。
- 前端修正：卡片同时展示招聘会状态和网申状态；默认筛选文案调整为“仅显示未结束”，避免把宣讲已结束但仍可网申的数据误称为未过期招聘会。
- 验证结果：`mvn -Dtest=EmploymentModuleIntegrationTest test` 通过，新增当前时间进行中状态与去重分页断言；`npm run build` 已通过；`git diff --check` 通过。
